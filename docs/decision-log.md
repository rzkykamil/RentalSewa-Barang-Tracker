# Decision Log — Rental Sewa Barang Tracker

Log keputusan arsitektur penting (format ADR ringan). Setiap entry baru ditambahkan di commit yang sama dengan perubahan terkait — lihat `.claude/rules/decision-logging.md`.

## [2026-07-20] Role tunggal per user (Owner / Renter / Admin), bukan dual-role bebas

- **Konteks:** brief awal menyebut "auth multi-role (pemilik barang & penyewa)" — perlu diputuskan apakah satu akun bisa jadi Owner sekaligus Renter secara bersamaan (model marketplace bebas ala Airbnb) atau role tetap per akun.
- **Keputusan:** setiap user punya satu `role` eksplisit (`OWNER`, `RENTER`, atau `ADMIN`) yang dipilih saat registrasi.
- **Alasan:** brief eksplisit memisahkan tanggung jawab "Pemilik: CRUD barang" vs "Penyewa: browse & request" sebagai dua peran berbeda. Role tunggal membuat RBAC (permission matrix, route group per role) jauh lebih sederhana untuk fase awal. Opsi dual-role (satu akun bisa keduanya) ditolak untuk Phase 1 karena menambah kompleksitas UI (switch role) dan permission checking tanpa manfaat yang terbukti dibutuhkan di awal.
- **Dampak:** kalau nanti ada kebutuhan satu orang jadi Owner sekaligus Renter, perlu migrasi skema (`role` jadi relasi many-to-many atau array) — dicatat sebagai Phase 2 di `docs/prd.md`.

## [2026-07-20] Lock ketersediaan barang berbasis status tunggal, bukan kalender rentang tanggal

- **Konteks:** perlu mekanisme mencegah barang fisik yang sama disewa dua penyewa dalam rentang tanggal yang tumpang tindih.
- **Keputusan:** barang hanya menerima request baru saat `status = TERSEDIA`. Begitu satu request di-approve, `status` berubah `DISEWA` dan seluruh request `PENDING` lain utk barang yang sama otomatis `REJECTED` (BR1 di `docs/prd.md`).
- **Alasan:** jauh lebih sederhana diimplementasikan & dipahami user dibanding kalender ketersediaan berbasis rentang tanggal (yang butuh UI kalender, validasi overlap kompleks). Cukup untuk skala Phase 1 di mana satu barang biasanya hanya disewa oleh satu orang dalam satu waktu.
- **Alternatif ditolak:** kalender ketersediaan penuh (mendukung booking di masa depan yang tidak overlap dengan booking aktif saat ini) — ditunda ke Phase 2 karena menambah kompleksitas signifikan tanpa terbukti dibutuhkan di awal.
- **Dampak:** Owner tidak bisa menerima request untuk tanggal jauh di masa depan selagi barang masih `DISEWA` untuk booking saat ini, meski secara tanggal sebenarnya tidak overlap — trade-off yang disadari.

## [2026-07-20] Storage foto barang di filesystem lokal, bukan cloud storage

- **Konteks:** butuh tempat menyimpan foto barang yang diunggah Owner. Cloud storage (Vercel Blob/Cloudinary/Supabase Storage) lebih scalable, tapi butuh setup akun & biaya pihak ketiga tambahan.
- **Keputusan:** foto disimpan di filesystem lokal server (volume Docker persisten), bukan cloud storage.
- **Alasan:** menyederhanakan setup awal tanpa dependency pihak ketiga tambahan, sesuai preferensi eksplisit saat brief. Trade-off yang disadari dan diterima.
- **Dampak — PENTING:** karena filesystem lokal dipakai, **deployment production wajib ke VPS + Docker dengan volume persisten**, bukan platform serverless (Vercel/Netlify) yang filesystem-nya ephemeral. Ini membatasi opsi deployment sejak awal (lihat `docs/technical-spec.md` & `docs/flows/system-architecture.md`). Migrasi ke cloud storage dicatat sebagai kandidat Phase 3 di `docs/prd.md` kalau kebutuhan skalabilitas/serverless muncul.

## [2026-07-20] Payment tracking manual, tanpa payment gateway di Phase 1

- **Konteks:** brief menyebut "payment tracking (manual: lunas/belum dulu)" — payment gateway otomatis bukan kebutuhan awal.
- **Keputusan:** Owner menandai status pembayaran (`BELUM_LUNAS`/`LUNAS`) secara manual per booking, tanpa integrasi payment gateway.
- **Alasan:** mengurangi kompleksitas & biaya integrasi (biaya transaksi gateway, kepatuhan PCI, dsb) di fase awal saat volume transaksi masih kecil.
- **Dampak:** tidak ada jaminan otomatis bahwa pembayaran benar-benar terjadi (rawan human error/kecurangan pencatatan) — mitigasi lewat riwayat transaksi yang transparan & rating/review. Integrasi Midtrans/Xendit dicatat sebagai Phase 2 di `docs/prd.md`.

## [2026-07-20] Reminder via email + in-app, tanpa WhatsApp/SMS di Phase 1

- **Konteks:** brief menyebut "reminder otomatis mendekati/lewat tenggat" tanpa menspesifikasikan channel.
- **Keputusan:** reminder dikirim via email (SMTP/Resend) dan notifikasi in-app, dijalankan oleh scheduled job worker terpisah dari siklus request utama.
- **Alasan:** email tidak butuh biaya per-pesan dan setup pihak ketiga tambahan dibanding WhatsApp/SMS (Fonnte/Twilio), cukup untuk memenuhi kebutuhan reminder di fase awal.
- **Dampak:** efektivitas reminder bergantung pada seberapa sering user mengecek email — kandidat penambahan channel WhatsApp/SMS dicatat sebagai Phase 2 di `docs/prd.md` kalau data menunjukkan open-rate email rendah.

## [2026-07-20] Modular monolith, bukan microservices

- **Konteks:** perlu menentukan gaya arsitektur backend untuk skala target awal (50 barang, 20 transaksi/3 bulan).
- **Keputusan:** satu aplikasi Next.js (App Router) menangani frontend & backend, kode dipisah per domain modul dengan boundary jelas.
- **Alasan:** microservices penuh menambah beban operasional (deployment terpisah, network overhead) yang tidak sepadan dengan skala & ukuran tim saat ini.
- **Dampak:** kalau salah satu modul (misal reminder job) perlu di-scale independen di masa depan, migrasi ke service terpisah tetap dimungkinkan berkat pemisahan modul yang sudah rapi — dicatat di `docs/technical-spec.md`.

## [2026-07-20] Design tokens dasar ditambahkan lebih awal di `globals.css` (bukan menunggu item cross-cutting terpisah)

- **Konteks:** komponen `src/components/ui/*` (shadcn) sudah digenerate dan wajib dipakai (`bg-primary`, `bg-destructive`, `bg-card`, `ring-ring`, dsb.), tapi `src/app/globals.css` belum mendefinisikan CSS variable-nya (`docs/todo/frontend.md` "Setup design tokens" masih belum dicentang). Tanpa token, semua komponen UI (Button, Input, Card, Select, dsb.) render tanpa warna sama sekali.
- **Keputusan:** menambahkan `@theme` token minimal (background/foreground/primary/secondary/muted/accent/destructive/border/input/ring + light & dark) langsung saat mengerjakan modul Auth (Periode 1), sekaligus menambahkan token warna status (`--color-status-positive/pending/active/late/inactive`) sesuai palet di `docs/design-system.md` §2 supaya siap dipakai `BookingStatusBadge` nanti.
- **Alasan:** ini prasyarat teknis, bukan pekerjaan modul lain — tanpa token, komponen yang diwajibkan dipakai di modul Auth tidak bisa direview secara visual sama sekali. Warna brand primer dipilih biru (`oklch(0.5 0.19 255)`) sesuai opsi di design-system.md.
- **Dampak:** item cross-cutting "Setup design tokens" di `docs/todo/frontend.md` sudah sebagian terpenuhi oleh perubahan ini — perlu dicek ulang & disesuaikan (bukan dibuat dari nol) saat modul Barang/Booking mengerjakan komponen status & card lain yang mungkin butuh token tambahan.

## [2026-07-20] Prisma Client generator baru (`prisma-client`) dengan driver adapter `@prisma/adapter-pg`

- **Konteks:** setup awal project memakai Prisma versi terbaru (7.8.0). Generator client default di versi ini (`prisma-client`, output ke `src/generated/prisma`) tidak lagi menyertakan query engine binary bawaan — client mengharuskan driver adapter eksplisit untuk terhubung ke database, dan koneksi DB tidak lagi dibaca otomatis dari `datasource.url` di `schema.prisma` (dipindah ke `prisma.config.ts` untuk kebutuhan CLI).
- **Keputusan:** pakai `@prisma/adapter-pg` (driver adapter resmi untuk `pg`) di `src/lib/prisma.ts`, diinisialisasi dengan `connectionString: process.env.DATABASE_URL`. `prisma.config.ts` dibuat memuat env dari `.env.local` (bukan `.env`) supaya konsisten dengan konvensi project.
- **Alasan:** ini adalah perilaku default versi terbaru Prisma yang diminta (bukan pilihan opsional) — driver adapter adalah jalur resmi Prisma untuk Postgres di generator baru, dan `pg` adalah driver Node.js Postgres paling umum dipakai.
- **Dampak:** `pg` & `@prisma/adapter-pg` jadi dependency wajib (bukan hanya `@prisma/client`). Kalau nanti pindah ke provider serverless Postgres (mis. Neon/Supabase), adapter perlu diganti ke driver yang sesuai (`@prisma/adapter-neon`, dsb) — perubahan terisolasi di `src/lib/prisma.ts`.

## [2026-07-20] Reminder in-app mock diturunkan dari `MOCK_BOOKINGS` dengan tanggal referensi tetap (`MOCK_TODAY`), bukan `Date.now()`

- **Konteks:** Modul Reminder frontend (`docs/todo/frontend.md`) butuh komponen badge/counter notifikasi & halaman daftar notifikasi in-app untuk reminder H-1 dan overdue, memakai mock data (belum ada `ReminderLog`/job worker sungguhan — lihat `docs/database-design.md` §`reminder_logs`). Tidak ada endpoint frontend-facing di `docs/api-spec.md` untuk ini (dicatat di `docs/todo/backend.md` Backlog).
- **Keputusan:** `src/lib/mock/reminders.ts` menghitung reminder (`H1_REMINDER` / `OVERDUE_ALERT`) secara derivatif dari booking `ACTIVE` di `src/lib/mock/bookings.ts`, dibandingkan terhadap konstanta tanggal tetap `MOCK_TODAY = "2026-07-20"` — bukan `new Date()`/`Date.now()` waktu nyata. Dua booking mock (`booking-3`, `booking-10`) disesuaikan/ditambahkan supaya masing-masing menghasilkan satu contoh H-1 dan satu contoh overdue, melibatkan `MOCK_USERS.OWNER` & `MOCK_USERS.RENTER` sekaligus (supaya kedua state terlihat di dashboard Owner maupun Renter). Reminder discope per user lewat `getRemindersForOwner`/`getRemindersForRenter` (pola sama seperti `getBookingsByOwner`/`getBookingsByRenter`), dan badge counter (`ReminderBell`) menghitung total dari situ — tidak ada state "sudah dibaca" karena skema `reminder_logs` saat ini tidak punya kolom tsb.
- **Alasan:** memakai tanggal referensi nyata (`Date.now()`) akan membuat demo H-1/overdue "basi" begitu tanggal kalender sungguhan bergeser dari tanggal saat data mock ditulis — reviewer yang membuka halaman ini beberapa minggu kemudian tidak akan melihat state yang dimaksud. Konstanta tetap membuat demo deterministik & dapat direview kapan saja, konsisten dengan pola modul lain yang memakai tanggal absolut hardcoded (mis. `src/lib/mock/bookings.ts`).
- **Dampak:** saat migrasi ke backend sungguhan (Periode 14), logic ini digantikan job worker (`worker/reminder-job.ts`, `docs/todo/backend.md`) yang menulis `ReminderLog` sungguhan berdasarkan `Date.now()` asli, dan frontend perlu endpoint `GET` baru (dicatat di `docs/todo/backend.md` Backlog) untuk membaca in-app reminder — `MOCK_TODAY` & `src/lib/mock/reminders.ts` dihapus saat itu.

## [2026-07-20] Mock booking data menambahkan timestamp `activatedAt` di luar skema `bookings` yang tercatat di `docs/database-design.md`

- **Konteks:** Modul Booking frontend butuh menampilkan timeline visual eksplisit PENDING → APPROVED → ACTIVE → COMPLETED (`docs/design-system.md` §1) berikut aksi Owner "tandai aktif" / "tandai selesai". Skema `bookings` di `docs/database-design.md` §2 hanya mendefinisikan `requested_at`, `approved_at`, `rejected_at`, `completed_at` — tidak ada kolom timestamp khusus untuk transisi APPROVED → ACTIVE, padahal endpoint `PATCH /api/v1/bookings/:id/activate` sudah direncanakan di `docs/todo/backend.md`.
- **Keputusan:** `src/lib/mock/bookings.ts` menambahkan field `activatedAt` (nullable) ke tipe `MockBooking`, di luar kolom yang tercatat saat ini di `docs/database-design.md`.
- **Alasan:** timeline UI perlu sumber data yang konsisten untuk menandai step "Berjalan" sebagai tercapai, dan business rule PRD (poin 6: "sistem harus mencatat setiap perubahan status booking dengan timestamp") mengisyaratkan transisi ke `ACTIVE` juga perlu dicatat waktunya, bukan hanya disimpulkan dari `status` saat ini.
- **Dampak:** ini murni keputusan data mock untuk fase frontend, belum mengubah `docs/database-design.md`. Ditandai di `docs/todo/backend.md` bagian Backlog/Temuan supaya kolom `activated_at` ditambahkan ke skema `bookings` saat migrasi Modul Booking backend dikerjakan.

## [2026-07-21] EXPERIMENT — Cross-cutting frontend (design tokens, `EmptyState`, responsive audit) dikerjakan tanpa supervisi langsung, di branch terpisah

- **CATATAN EKSPERIMEN:** entry ini mendokumentasikan pekerjaan yang dijalankan sebagai **eksperimen** — dikerjakan otomatis oleh agent tanpa supervisi langsung user (user sedang tidur saat sesi ini berjalan), di branch `TEST-AJA` (terpisah dari `main`), **menunggu review manual user besok pagi** sebelum dianggap final atau di-push/merge. Jangan menganggap keputusan di bawah ini sudah final sampai direview.
- **Konteks:** tiga item "Cross-cutting" di `docs/todo/frontend.md` (setup design tokens, komponen shared `EmptyState`, responsive check seluruh halaman utama) belum dicentang meski seluruh modul (Auth–Review) sudah selesai fase frontend.
- **Keputusan:**
  1. **Design tokens** — diverifikasi terhadap `docs/design-system.md` §2/§4: token di `src/app/globals.css` (lihat entry `[2026-07-20] Design tokens dasar...`) sudah lengkap (brand primary, status colors, radius, font, dark mode) dan konsisten dipakai (tidak ada styling status ad-hoc ditemukan di halaman manapun). Tidak ada perubahan kode diperlukan — dicentang selesai berdasarkan verifikasi, bukan implementasi baru.
  2. **`EmptyState`** — komponen baru `src/components/shared/EmptyState.tsx` dengan API `{ icon?: LucideIcon, title: string, description: string, action?: React.ReactNode, className?: string }`. `icon` opsional (bukan wajib) karena banyak empty-state existing (list kosong, hasil filter kosong) tidak punya ilustrasi/icon di desain saat ini — memaksa icon wajib akan memaksa pemilihan icon arbitrer di semua pemanggilan. `action` bertipe `ReactNode` (bukan `{ label, onClick }`) supaya pemanggil bebas memakai `Button`+`Link` (navigasi) atau `Button`+handler (aksi client) sesuai konteks, konsisten dengan pola `ConfirmDialog.trigger`. Delapan pemakaian empty-state ad-hoc (`Card`+`CardContent` dengan judul+deskripsi manual, diulang identik di banyak halaman) direfactor untuk memakai komponen ini: `owner/items`, `owner/bookings`, `renter/bookings`, `BrowseItemsView`, `TransactionHistoryList` (2 varian: tanpa riwayat & tanpa hasil filter), `ReminderList`, dan halaman "barang tidak ditemukan" (`renter/browse/[id]`, `renter/browse/[id]/request`). Empty-state ringkas non-Card (mis. teks satu baris riwayat barang di `owner/items/[id]`, "Belum ada rating" di detail barang, dropdown `ReminderBell`) sengaja **tidak** direfactor karena tidak memakai pola Card+judul+deskripsi yang sama — memaksakan `EmptyState` di situ akan menambah bobot visual yang tidak sesuai konteks compact tsb.
  3. **Responsive audit** — meninjau seluruh halaman utama (dashboard, browse, detail barang, booking Owner/Renter, history, notifications, form barang/booking) terhadap breakpoint `docs/design-system.md` §4. Semua sudah mengikuti konvensi yang konsisten (grid `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` di Browse, tabel dengan kolom `hidden sm:table-cell` + wrapper `overflow-x-auto` bawaan komponen `Table`, tombol aksi `flex-col-reverse sm:flex-row`, `DashboardShell` dengan nav horizontal-scroll di mobile). Tidak ditemukan halaman yang butuh perbaikan responsive — dicentang selesai berdasarkan audit, tanpa perubahan kode.
- **Alasan:** ketiga item ini murni cross-cutting (bukan fitur baru per modul) sehingga cocok dikerjakan sebagai satu batch terpisah dari alur modul biasa. Eksperimen dijalankan otomatis untuk menguji apakah agent bisa menuntaskan pekerjaan audit + refactor cross-cutting berisiko rendah (tidak mengubah business logic, hanya UI shared) secara mandiri semalaman tanpa pengawasan real-time.
- **Dampak:** karena ini eksperimen tanpa supervisi, **user wajib review manual** perubahan di branch `TEST-AJA` besok pagi (terutama tampilan visual `EmptyState` di semua halaman yang direfactor) sebelum di-push atau di-merge ke `main`. Tidak ada perubahan skema data, endpoint, atau business rule — risiko terbatas pada regresi visual/markup di halaman yang disebut di atas. `npm run lint` sudah dijalankan dan tidak ada error (1 warning pre-existing di `PhotoUploadPreview.tsx`, tidak terkait perubahan ini).

## [2026-07-21] Foundation Backend Auth: NextAuth route di luar `/api/v1`, role mismatch di middleware redirect ke `/`, module augmentation NextAuth di file terpisah

- **Konteks:** implementasi foundation backend Modul Auth (schema `User`, NextAuth credentials provider, middleware role guard, seed data) sesuai `docs/todo/backend.md` dan `docs/flows/auth-permission-flow.md`.
- **Keputusan:**
  1. **Lokasi NextAuth**: dipakai jalur standar App Router `src/app/api/auth/[...nextauth]/route.ts` (bukan di bawah `/api/v1/auth/**`), dengan config terpisah di `src/modules/auth/auth-options.ts` dan business logic verifikasi kredensial di `src/modules/auth/auth.service.ts`.
  2. **Middleware role guard** (`src/middleware.ts`) hanya menangani cek session valid + role-level match untuk prefix `/owner`, `/renter`, `/admin` — kalau role tidak cocok, redirect ke `/` (bukan render halaman 403). Ownership check (`resource.owner_id === session.user.id`) sengaja tidak di sini, tetap di service/handler layer per `.claude/rules/api-design.md`.
  3. **Module augmentation NextAuth v4** (`Session.user.id`, `Session.user.role`, dst) ditaruh di `src/types/next-auth.d.ts` terpisah, bukan colocated di `auth-options.ts`.
- **Alasan:**
  1. NextAuth punya jalur URL bawaannya sendiri (`/api/auth/*`) yang terpisah dari skema versioning custom `/api/v1` — endpoint `POST /api/v1/auth/login` di `docs/api-spec.md` secara konsep *adalah* callback credentials NextAuth ini, bukan route handler custom tambahan.
  2. Redirect ke `/` dipilih karena ini route halaman (bukan API) — 403 mentah kurang ramah UX dibanding kembali ke landing page; belum ada halaman `/forbidden` khusus di fase frontend saat ini.
  3. File `.d.ts` terpisah membuat TypeScript otomatis pick up augmentation secara global tanpa perlu di-import eksplisit di setiap file yang memakai `Session`/`JWT`.
- **Dampak:** kalau nanti dibuat halaman `/forbidden` khusus di fase frontend, middleware perlu diupdate untuk redirect ke sana alih-alih `/`. Seed data (`prisma/seed.ts`, password default `Admin123!`/`Owner123!`/`Renter123!`) belum dijalankan ke DB — perlu `npm run db:seed` manual oleh user. Endpoint `POST /api/v1/auth/register` dan `GET/PATCH /api/v1/auth/me` masih backlog terpisah, belum dikerjakan di foundation ini.

## [2026-07-23] Modul Barang backend: `uploads/items/<itemId>/<uuid>.<ext>` storage layout, dedicated `/uploads/**` streaming route, item id generated up-front for atomic photo+row creation

- **Konteks:** implementasi tiga item "Modul Barang (Item)" di `docs/todo/backend.md`: `POST/PATCH/DELETE /api/v1/items`, `GET /api/v1/items` (+`:id`), dan handler upload foto ke filesystem lokal, mengikuti `docs/technical-spec.md` §10 (`./uploads`, gitignored) dan ADR "Storage foto barang di filesystem lokal".
- **Keputusan:**
  1. **Layout folder:** `uploads/items/<itemId>/<uuid>.<ext>` (bukan `public/uploads/...`) — di luar `public/` supaya file tidak diperlakukan sebagai build-time static asset dan konsisten dengan asumsi volume Docker terpisah di production.
  2. **Route serving baru** `src/app/uploads/[...path]/route.ts` (GET, publik, tanpa auth) membaca file dari `UPLOAD_ROOT` (`src/lib/upload.ts`) dan mengembalikan bytes dengan `Content-Type` sesuai ekstensi whitelist (`jpg/jpeg/png/webp`). Sengaja **tidak** ditaruh di bawah `/api/v1` karena bukan JSON API (tidak memakai envelope `{data}`/`{error}`) — analog static asset. Path traversal dicegah dengan menolak segmen `.`/`..` dan memverifikasi hasil `path.join` masih di dalam `UPLOAD_ROOT`.
  3. **Validasi foto** (`src/lib/upload.ts`): whitelist MIME `image/jpeg`, `image/png`, `image/webp`; batas ukuran **5MB per file** dan **maksimum 8 foto per barang** — keduanya asumsi (tidak ada angka spesifik di dokumen manapun), didokumentasikan sebagai konstanta bernama supaya mudah disesuaikan.
  4. **Item id di-generate di service (`randomUUID()`) sebelum insert DB**, supaya folder `uploads/items/<itemId>/` bisa dipakai untuk menyimpan file *sebelum* row `Item`/`ItemPhoto` ditulis — insert `Item` + `ItemPhoto` (multi-tabel) tetap dibungkus `prisma.$transaction` sesuai `.claude/rules/api-design.md`, tapi penulisan file ke disk terjadi di luar transaction DB (tidak bisa di-rollback bareng). Foto pertama yang diunggah otomatis ditandai `isPrimary`.
  5. **`ratingAverage` di `GET /items/:id` selalu `null`** — model `Booking`/`Review` belum ada (Periode 8/14), field disiapkan di response shape tapi nilainya di-hardcode dengan komentar TODO di `items.service.ts`, bukan menunda seluruh field.
  6. **`PATCH /items/:id` tidak menerima upload foto** (JSON body saja) — tambah/hapus foto pada barang yang sudah ada belum dispesifikasikan di `docs/api-spec.md`, jadi sengaja dibatasi ke field teks (name/description/category/condition/pricePerDay) untuk sekarang.
- **Alasan:** memisahkan file serving dari `/api/v1` menjaga konvensi bahwa semua yang di bawah `/api/v1` selalu JSON envelope; folder di luar `public/` menghindari file foto ikut ter-bundle sebagai static asset build time dan konsisten dengan constraint volume Docker persisten yang sudah diputuskan. Generate id lebih awal menyederhanakan penamaan folder tanpa perlu langkah "rename folder setelah insert".
- **Dampak:** kalau ada request gagal di tengah jalan (mis. `ItemPhoto.createMany` gagal setelah file sempat ditulis ke disk), file yang sudah ditulis jadi orphan — tidak ada cleanup otomatis di Phase 1 (trade-off yang disadari, risiko rendah karena volume kecil). Endpoint `GET /api/v1/items/:id/bookings` tetap belum diimplementasikan (butuh model `Booking`) — sudah tercatat sebagai item terpisah di `docs/todo/backend.md` bagian Modul History.

## [2026-07-23] Modul Auth backend: register/me endpoints, login rate limiting wraps NextAuth route (not `authorize()`), fixed-window in-memory limiter, new `429 RATE_LIMITED` error code

- **Konteks:** implementasi tiga item "Modul Auth" di `docs/todo/backend.md`: `POST /api/v1/auth/register`, `GET/PATCH /api/v1/auth/me`, dan rate limiting (~5 percobaan/menit per IP) untuk login & register (`docs/technical-spec.md` §6).
- **Keputusan:**
  1. **Rate limiter in-memory berbasis `lru-cache`**, fixed-window counter (bukan token bucket/sliding window): key cache menyertakan indeks window 60 detik (`Date.now() / 60_000`), sehingga window lama otomatis kadaluarsa dari LRU cache tanpa bookkeeping manual. Diimplementasikan di `src/lib/rate-limit.ts` (`checkRateLimit`, `getClientIp`), dipakai di `POST /api/v1/auth/register` dan login.
  2. **Rate limiting login TIDAK ditaruh di callback `authorize()`** (`src/modules/auth/auth-options.ts`), melainkan di wrapper `POST` pada `src/app/api/auth/[...nextauth]/route.ts` yang mencegat khusus path `.../callback/credentials`. Alasan: NextAuth v4 selalu mengembalikan `401` generik untuk kegagalan `authorize()` (return `null`/throw), tidak ada jalur bagi `authorize()` untuk mengontrol status HTTP kustom seperti `429` — intercept di route handler adalah satu-satunya tempat yang bisa mengembalikan response `429` dengan format error standar sebelum handler NextAuth berjalan.
  3. **Response error `429 RATE_LIMITED` ditambahkan** ke tabel status/error code di `docs/api-spec.md` §2 — sebelumnya belum terdaftar (hanya 400/401/403/404/409/422/500).
  4. **Registrasi hanya menerima role `OWNER`/`RENTER`** lewat Zod enum eksplisit (`z.enum(["OWNER", "RENTER"])`) — `ADMIN` tidak bisa diset dari endpoint publik ini sama sekali (bukan divalidasi lalu ditolak, tapi memang tidak ada di union type yang diterima parser).
  5. **`GET/PATCH /auth/me`** memakai `getServerSession(authOptions)` (helper resmi `next-auth`, tidak ada helper session existing di project untuk direuse) untuk cek login; profil (`name`, `phone`, `avatarUrl`) yang bisa diubah via `PATCH` dibatasi eksplisit di Zod schema — email/role/password sengaja tidak bisa diubah lewat endpoint ini.
- **Alasan:** konsisten dengan constraint deployment single-instance Phase 1 (`docs/technical-spec.md` §13) yang membuat Redis-based limiter belum perlu; fixed-window dipilih karena implementasinya jauh lebih sederhana dibanding token bucket sungguhan untuk kebutuhan batas kasar 5/menit, trade-off (burst di batas window) dianggap dapat diterima di skala Phase 1.
- **Dampak:** rate limiter ini **tidak aman lintas multi-instance** (tiap proses Node punya cache in-memory sendiri) — kalau nanti horizontal scaling diterapkan (`docs/technical-spec.md` §13), limiter perlu dipindah ke store bersama (Redis) sebelum multi-instance production. Komentar `NOTE` lama di `src/modules/auth/auth.service.ts` yang menyatakan register adalah backlog terpisah sudah dihapus dan digantikan implementasi `registerUser()`/`getUserProfile()`/`updateUserProfile()`.

## [2026-07-21] EXPERIMENT — Modul Admin frontend (Kelola User/Barang/Booking) dikerjakan tanpa supervisi langsung, di branch terpisah

- **CATATAN EKSPERIMEN:** entry ini mendokumentasikan pekerjaan yang dijalankan sebagai **eksperimen** — dikerjakan otomatis oleh agent tanpa supervisi langsung user (user sedang tidur saat sesi ini berjalan), di branch `TEST-AJA` (terpisah dari `main`), **menunggu review manual user besok pagi** sebelum dianggap final atau di-push/merge. Jangan menganggap keputusan di bawah ini sudah final sampai direview. Scope pekerjaan ini murni **frontend** (UI + mock data) — tidak menyentuh backend/API/Prisma, meski Periode 16 di `docs/development-workflow.md` menggabungkan frontend+backend Panel Admin.
- **Konteks:** tiga item "Modul Admin" di `docs/todo/frontend.md` (halaman daftar user + nonaktifkan, halaman daftar barang + nonaktifkan paksa, halaman daftar booking read-only) belum dikerjakan. `src/lib/mock/session.ts` hanya punya satu `MockUser` per role (dipakai sebagai "user yang sedang login" per dashboard shell), tidak cukup untuk halaman admin "daftar seluruh user" — nama owner/renter lain yang direferensikan `MOCK_ITEMS`/`MOCK_BOOKINGS` (`OTHER_OWNERS`, `OTHER_RENTERS`) hanya berupa `{id, name}`, bukan `MockUser` lengkap dengan email/phone. Field `isActive` (dibutuhkan untuk aksi nonaktifkan) juga belum ada di `MockUser`.
- **Keputusan:**
  1. **Agregasi user admin di file baru, bukan extend `MockUser` global** — dibuat `src/lib/mock/admin.ts` berisi `AdminUser extends MockUser { isActive: boolean }` dan `ALL_USERS: AdminUser[]`, menggabungkan `MOCK_USERS.OWNER/RENTER/ADMIN` dengan `OTHER_OWNERS` (di `items.ts`) dan `OTHER_RENTERS` (di `bookings.ts`) — dua const tsb diubah jadi `export` (tanpa mengubah isi/urutan) supaya bisa diimpor, bukan diduplikasi manual. Email/phone dibuat masuk akal secara deterministik dari nama (`nama.toLowerCase().replace(/\s+/g,".")@example.com`). `MockUser` di `session.ts` **tidak diubah** — modul lain (Auth, dashboard shell Owner/Renter) yang sudah selesai di fase sebelumnya bergantung pada bentuk `MockUser` saat ini, jadi extend lewat tipe baru di file terpisah menghindari risiko regresi ke modul-modul tsb. `MOCK_ITEMS`/`MOCK_BOOKINGS` sendiri **tidak diduplikasi** — dua halaman admin lain (`admin/items`, `admin/bookings`) mengimpor langsung dari `src/lib/mock/items.ts`/`bookings.ts`.
  2. **Tiga halaman baru** — `src/app/(admin)/admin/users/page.tsx` (tabel `ALL_USERS`, aksi nonaktifkan/aktifkan lewat `ConfirmDialog`, state lokal di `AdminUsersTable`), `src/app/(admin)/admin/items/page.tsx` (tabel `MOCK_ITEMS`, aksi "Nonaktifkan Paksa" mengubah `status` jadi `NONAKTIF` di state lokal `AdminItemsTable`, tombol disembunyikan/diganti teks kalau barang sudah nonaktif), `src/app/(admin)/admin/bookings/page.tsx` (read-only, server component langsung tanpa state karena tidak ada aksi — beda dari dua halaman lain yang butuh client component untuk mutasi lokal). Badge status memakai komponen yang sudah ada (`ItemStatusBadge`, `BookingStatusBadge`) plus badge baru `UserStatusBadge` (Aktif/Nonaktif) yang mengikuti pola mapping warna+label internal yang sama, memakai token `--status-positive`/`--status-inactive` yang sudah ada — bukan warna ad-hoc baru.
  3. **Nav & dashboard admin diaktifkan** — `comingSoon: true` dihapus dari 3 entry admin di `src/lib/dashboard/nav-config.ts`. `src/app/(admin)/admin/dashboard/page.tsx` diubah dari summary card placeholder `"—"` jadi angka asli dihitung dari `ALL_USERS.length`/`MOCK_ITEMS.length`/`MOCK_BOOKINGS.length`/jumlah `isActive === false`, dan Card "Modul pengawasan Admin belum tersedia" dihapus.
- **Alasan:** eksperimen dijalankan otomatis untuk menguji apakah agent bisa menuntaskan satu modul frontend end-to-end (termasuk keputusan agregasi data mock lintas file) secara mandiri semalaman tanpa pengawasan real-time, mengikuti disiplin fase (frontend-only, tidak mencicil backend Periode 16) dan konvensi yang sudah mapan dari modul-modul sebelumnya (pola list+`ConfirmDialog`+`EmptyState` dari `owner/items`, pola badge status dari `ItemStatusBadge`/`BookingStatusBadge`).
- **Dampak:** karena ini eksperimen tanpa supervisi, **user wajib review manual** perubahan di branch `TEST-AJA` besok pagi (terutama data email/phone yang dikarang untuk `OTHER_OWNERS`/`OTHER_RENTERS`, dan keputusan menaruh `isActive` di tipe terpisah alih-alih di `MockUser` global) sebelum di-push atau di-merge ke `main`. Tidak ada perubahan skema data, endpoint, atau business rule sungguhan — seluruh mutasi (nonaktifkan user/barang) hanya state React lokal, hilang saat reload. Backend Modul Admin (endpoint `PATCH` nonaktifkan user/barang sungguhan, kolom `is_active` di skema `users`) masih perlu dikerjakan terpisah di sisi backend sesuai `docs/todo/backend.md`. `npm run lint` dijalankan di akhir sesi ini — lihat commit/PR terkait untuk hasilnya.

## [2026-07-23] Tambah kolom `bookings.activated_at` yang belum ada di skema desain awal

- **Konteks:** skema awal tabel `bookings` di `docs/database-design.md` §2 hanya punya `requested_at`/`approved_at`/`rejected_at`/`completed_at`, tidak ada timestamp khusus untuk transisi `APPROVED` → `ACTIVE`, padahal endpoint `PATCH /api/v1/bookings/:id/activate` sudah direncanakan sejak awal di `docs/todo/backend.md`. Gap ini ditemukan oleh sisi frontend saat mengerjakan mock data (`src/lib/mock/bookings.ts` menambahkan field `activatedAt` di luar skema tercatat, lihat entry `[2026-07-20] Mock booking data menambahkan timestamp activatedAt...`) dan dicatat sebagai temuan backlog di `docs/todo/backend.md`.
- **Keputusan:** menambahkan kolom `activated_at` (nullable, `timestamptz`) ke model `Booking` saat implementasi Prisma schema Modul Booking (Periode 8), diletakkan setelah `approved_at` di ERD/skema tabel `docs/database-design.md`.
- **Alasan:** dibutuhkan timestamp eksplisit untuk mencatat kapan booking beralih dari `APPROVED` ke `ACTIVE`, konsisten dengan pola `approved_at`/`rejected_at`/`completed_at` yang sudah ada untuk transisi status lain — tanpa kolom ini, waktu aktivasi hanya bisa disimpulkan dari `updated_at` yang tidak spesifik per transisi.
- **Dampak:** tidak ada breaking change — murni penambahan kolom nullable baru ke tabel `bookings`. Temuan terkait di `docs/todo/backend.md` bagian Backlog/Temuan sudah dihapus karena selesai ditindaklanjuti di commit ini.
